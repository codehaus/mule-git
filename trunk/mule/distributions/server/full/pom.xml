<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.mule.distributions</groupId>
        <artifactId>mule-server</artifactId>
        <version>1.4.2-SNAPSHOT</version>
    </parent>
    <artifactId>mule-full</artifactId>
    <!-- This should be "pom" not "jar" but if it is set to "pom", the list of dependencies gets ignored
    by the assembly plugin. -->
    <packaging>jar</packaging>
    <name>Full Distribution</name>
    <description>Includes the Mule server with all of its modules and transports.</description>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <!--
                    Don't upgrade because of http://jira.codehaus.org/browse/MASSEMBLY-194,
                    Needs to be fixed and then assembly descriptors should be updated.
                 -->
                <version>2.1</version>
                <configuration>
                    <finalName>mule-${version}</finalName>
                    <descriptors>
                        <descriptor>assembly.xml</descriptor>
                    </descriptors>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>assembly</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!--
                Create a dummy javadoc folder so the assembly always succeeds
                even without a full site run.
             -->
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>generate-sources</phase>
                        <configuration>
                            <tasks>
                                <mkdir dir="../../../target/site/apidocs"/>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <!--
                This is currently disabled until the assembly-whitelist.txt is populated.
                The format is:
                    1. Each line corresponds to a single library entry.
                    2. Each line contains an expected library jar name.
                    3. Library names should have no extension (".jar" stripped)
                    4. For Mule libraries *strip* the version completely. It will be derived from
                       the project version.
                    5. For 3rd-party libraries leave the version in (only strip the jar suffix from #3).
             -->
            <plugin>
                <groupId>org.mule.tools</groupId>
                <artifactId>mule-assembly-verifier</artifactId>
                <version>1.0</version>
                <executions>
                    <execution>
                        <phase>verify</phase>
                        <goals>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <projectOutputFile>mule-${version}.zip</projectOutputFile>
                    <!-- Default is under src/main/resources, pull it to the root for this project -->
                    <whitelist>assembly-whitelist.txt</whitelist>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>release-profile</id>
            <activation>
                <activeByDefault>false</activeByDefault>
                <property>
                    <name>performRelease</name>
                    <value>true</value>
                </property>
            </activation>

            <build>
                <plugins>
                    <plugin>
                        <groupId>org.mule.tools</groupId>
                        <artifactId>mule-assembly-verifier</artifactId>
                        <version>1.0</version>
                        <configuration>
                            <projectOutputFile>mule-${version}.zip</projectOutputFile>
                            <!-- Default is under src/main/resources, pull it to the root for this project -->
                            <whitelist>assembly-release-whitelist.txt</whitelist>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    
</project>
