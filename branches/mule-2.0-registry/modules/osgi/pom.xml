<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.mule.modules</groupId>
        <artifactId>mule-modules</artifactId>
        <version>2.0-REGISTRY</version>
    </parent>
  <artifactId>mule-module-osgi</artifactId>
  <packaging>osgi-bundle</packaging>
  <name>OSGi Support</name>
  <description>Classes used for Mule's OSGi support</description>

    <build>
         <plugins>
              <plugin>
                <groupId>org.apache.felix.plugins</groupId>
                <artifactId>maven-osgi-plugin</artifactId>
                <configuration>
                    <osgiManifest>
                        <!-- TODO This list should ideally be in MANIFEST.MF, but the maven-osgi-plugin can't handle it because it's too long. -->
                        <importPackage>org.apache.commons.logging;provider=paxlogging, org.mule, org.mule.umo, org.mule.umo.endpoint, org.mule.umo.routing, org.mule.umo.provider, org.mule.umo.lifecycle, org.mule.umo.model, org.mule.umo.transformer, org.mule.umo.manager, org.mule.util</importPackage>
                     </osgiManifest>
                </configuration>
            </plugin>
         </plugins>
    </build>

  <dependencies>
        <dependency>
            <groupId>org.mule.modules</groupId>
            <artifactId>mule-module-boot</artifactId>
            <version>${version}</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
             <groupId>org.knopflerfish</groupId>
             <artifactId>osgi</artifactId>
             <version>2.0.1</version>
        </dependency>
  </dependencies>
</project>
