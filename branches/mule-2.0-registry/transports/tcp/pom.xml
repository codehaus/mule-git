<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.mule.transports</groupId>
        <artifactId>mule-transports</artifactId>
        <version>2.0-REGISTRY</version>
    </parent>
    <artifactId>mule-transport-tcp</artifactId>
    <packaging>osgi-bundle</packaging>
    <name>Tcp Transport</name>
    <description>A Mule transport for Tcp Connectivity. This transport supplies a tcp Server and client implementation.</description>

    <build>
     	<plugins>
	      	<plugin>
		        <groupId>org.apache.felix.plugins</groupId>
		        <artifactId>maven-osgi-plugin</artifactId>
		        <configuration>
		            <osgiManifest>
		            	<!-- TODO This list should ideally be in MANIFEST.MF, but the maven-osgi-plugin can't handle it because it's too long. -->
						<importPackage>org.apache.commons.logging;provider=paxlogging, edu.emory.mathcs.backport.java.util.concurrent, edu.emory.mathcs.backport.java.util.concurrent.atomic, org.apache.commons.collections, org.mule.config.i18n, org.mule.impl, org.mule.providers, org.mule.transformers.simple, org.mule.umo, org.mule.umo.endpoint, org.mule.umo.lifecycle, org.mule.umo.manager, org.mule.umo.provider, org.mule.umo.transformer, org.mule.util</importPackage>
 		            </osgiManifest>
		        </configuration>
		    </plugin>
     	</plugins>
    </build>    
</project>
